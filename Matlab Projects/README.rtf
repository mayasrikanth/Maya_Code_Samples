

README for Matlab Projects
- .m file is an implementation of the k-means clustering algorithm on 2-d data\
- "FirstIteration.png" is the unlabeled clustering obtained after the first iteration the algorithm\
- "LastIteration.png" shows the unlabeled clustering obtained after convergence\
- "Matlab-KMeans" shows the unlabeled clustering produced by Matlab's built-in K-Means algorithm.}
