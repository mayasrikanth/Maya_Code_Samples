README:

This folder includes an implementation of a quaternion class to enable rotating and manipulating an object rendered by OpenGL. It
also includes code to parse a text file and use openGL API to render
the scene with gouraud shading. 